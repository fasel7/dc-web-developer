let try22: string = "test";

let test: number | string = 1234;

const test2: string = "der";

const testy50: string = "Julian";

test = "er12345672332";

function product(id: number, name: string, price: number) {
  const amount = 0;
  return { id, name, price };
}

let myProd = product(34, "kole", 60);
myProd.price = 12;
